(() => {
    const hero:[string,number] = ['Superman', 100]; // first element is a string, second is a number ALWAYS!!!
    const hero1:[string,number,boolean] = ['Superman', 100, true]; // first element is a string, second is a number, third is a boolean
    // hero1[3] = false; // error, the array is fixed to 3 elements
})();
